--------> Introduction To HTML
          --------> HTML is stand for Hyper Text Markup Language.
          --------> HTML Creates a basic Skeleton for Our Website.
          --------> Boiler Plate of basic html.
                    <code>
                    
                    <!DOCTYPE html>
                                <html>
                                <head>
                                <title>Page Title</title>
                                </head>
                                <body>

                                <h1>This is a Heading</h1>
                                <p>This is a paragraph.</p>

                                </body>
                                </html>
                                                    
                    </code>



          --------> About tags In Html.
                     -> Html{
                        --> The <html> tag represents the root of an HTML document.
                        --> The <html> tag is the container for all other HTML elements.
                     }.

                     -> Head{
                        --> The <head> element is a container for metadata (data about data) and is placed between the <html> tag and the <body> tag.
                        --> Metadata is data about the HTML document. Metadata is not displayed.
                        --> Metadata typically define the document title, character set, styles, scripts, and other meta information.
                     }.

                     -> Title{
                        --> The <title> tag defines the title of the document. The title must be text-only, and it is shown in the browser's title bar or in the page's tab.
                        -> The <title> tag is required in HTML documents!.
                        -> The contents of a page title is very important for search engine optimization (SEO)! The page title is used by search engine algorithms to decide the order when listing pages in search results.
                     }.

                     -> Style{
                        --> The <style> tag is used to define style information (CSS) for a document.
                        --> Inside the <style> element you specify how HTML elements should render in a browser.
                     }.

                     -> Body{
                        --> The <body> tag defines the document's body.
                        --> The <body> element contains all the contents of an HTML document, such as headings, paragraphs, images, hyperlinks, tables, lists, etc.
                     }.

                     -> Br{
                        --> <Br> Tag is just for line break.
                        *Exception.
                        --> don't have Close tag.
                     }.

                     -> B{
                        --> <B> tag is for bolding head or para.
                     }.

                     -> I{
                        --> <I> tag is for Italic head or para.
                     }.

                     -> Button{
                        --> for Creating buttons in website.
                     }.

                     -> Img{
                        --> For rendering Images in website.
                     }.

                     -> Table{
                        --> For making table in website.
                        --> by using tr tag, th tag and td tag.
                     }
                     -> iframe{
                        --> for rendering ather site on our site we use iframe tag.
                     }
                     -> sub{
                        --> Sub tag is for writing text below the letter.
                     }
                     -> sup{
                        --> sup tag is for writing text above the letter. 
                     }
                     -> strike{
                        --> cancelation effect or overline effect in text.
                     }

